# ACF JSON

Este directorio contiene los grupos de campos de ACF exportados en formato JSON.
Se sincroniza automáticamente al guardar/editar campos en WP Admin.

**Versionar este directorio** permite que los campos ACF se mantengan sincronizados
entre entornos (local, staging, producción) sin importar/exportar manualmente.
